print("Mary had a little lamb.") #just a text export by fonction print
print("Its fleece was white as {}" .format('snow')) # the previous talk about format is just format is not for action like (false) not that false now we don't sure but you should add ({}) with format for pc export the sentence after (format)
print("And everywhere that Mary went.")# other text ~_~
print("." * 10) # what'd that do ? # you know  that is method to multipe a string by use after the sting or ord the function (*) With the number you want to repeat according to its amount

end1 = "C" # is variable present a string and this string is a simple alphabet 
end2 = "h"# the same
end3 = "e"#the same 
end4 = "e" # the same 
end5 = "s"# the same 
end6 = "e"# the same 
end7 = "B"# the same 
end8 = "u"# the same 
end9 = "r"# the same 
end10 = "g"# the same 
end11 = "e"# the same 
end12 = "r"# the same 

# watch end = ' ' at the end. try removing it to see what happens
print(end1 + end2 + end3 + end4 + end5 + end6, end=' ') # firstly we want print this string variable with addition like you gathering تجمع  the alphabet wthout space between them 
# that the final you see that function (end= '' ) is amazing function can fusion the firt line and the second  line and you  can enter aynhing under like ('space') or (',')
print(end7 + end8 + end9 + end10 + end11 + end12)